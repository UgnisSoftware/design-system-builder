const parts = ["label", "requiredIndicator"]

const baseStyle = {
  label: {
    color: "neutral.800",
    mb: 0.25,
    fontSize: "xs",
    lineHeight: "0.75rem",
  },
  requiredIndicator: {
    ml: "1px",
    color: "error.700",
    fontSize: "sm",
    lineHeight: 0,
  },
}

export default {
  baseStyle,
  parts,
}
