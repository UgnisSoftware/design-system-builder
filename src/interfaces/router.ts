export enum RouterPaths {
  fonts = 'fonts',
  colors = 'colors',
  elements = 'elements',
  components = 'components',
  exporting = 'exporting',
  assets = 'assets',
}

export enum ElementRoutes {
  Buttons = 'Buttons',
  Inputs = 'Inputs',
  Popups = 'Popups',
}
