export enum RouterPaths {
  fonts = 'fonts',
  colors = 'colors',
  components = 'components',
  exporting = 'exporting',
  assets = 'assets',
  buttons = 'buttons',
  inputs = 'inputs',
  popups = 'popups',
}