export enum RouterPaths {
  fonts = 'fonts',
  colors = 'colors',
  components = 'components',
  exporting = 'exporting',
  assets = 'assets',

  // Elements
  buttons = 'buttons',
  inputs = 'inputs',
  popups = 'popups',
}

export enum ElementRoutes {

}
